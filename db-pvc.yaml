piVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vpro-db-data-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  storageClassName: standard
  volumeMode: Filesystem
  volumeName:  vpro-db-data
